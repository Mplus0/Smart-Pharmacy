# 任务：整理当前 src 文件夹下所有 ROS 功能包，并补充当前 README.md

你现在需要阅读我当前 ROS 工作空间中 `src` 文件夹下的所有功能包，帮助我整理一份清晰、准确、方便其他队友理解和使用的 README 文档。

请注意：本次任务不是重构代码，也不是修改功能逻辑，而是**阅读代码结构、梳理功能包关系，并修改/补充当前文件夹下已有的 README.md 文件**。

---

## 一、项目背景

这是一个用于“智慧药房”比赛的 ROS 小车项目。

项目中可能包含多个 ROS 功能包，例如：

- 主控流程功能包
- 导航相关功能包
- 视觉识别功能包
- 语音播报功能包
- TCP / 裁判系统通信功能包
- 调试工具功能包
- launch 启动文件
- config 参数配置文件
- scripts Python 节点

请你根据实际仓库内容判断，不要凭空编造不存在的功能包或文件。

---

## 二、你的工作目标

请你完成以下任务：

1. 扫描当前 `src` 文件夹下的所有 ROS 功能包。
2. 阅读每个功能包的：
   - `package.xml`
   - `CMakeLists.txt`
   - `launch/`
   - `scripts/`
   - `src/`
   - `config/`
   - `msg/`
   - `srv/`
   - `README.md`
   - 其他关键文件
3. 梳理每个功能包的作用。
4. 梳理主要节点、话题、服务、参数、launch 文件之间的关系。
5. 总结项目整体运行流程。
6. 总结常用启动命令和调试命令。
7. 修改或补充当前文件夹下已有的 `README.md` 文件。
8. 如果当前 README.md 已经有内容，请在原有内容基础上补充和优化，不要直接全部覆盖，除非原文件内容明显错误或重复混乱。

---

## 三、非常重要的要求

请严格遵守以下要求：

1. **不要修改任何代码逻辑。**
2. **不要修改 launch、Python、YAML、CMakeLists、package.xml 等功能文件。**
3. 本次只允许修改 README 文档。
4. 不要凭空猜测功能。
5. 每一个功能说明都必须尽量基于实际文件、实际节点、实际 launch 和实际配置。
6. 如果某个功能无法从代码中确认，请写成“可能用于……”或“根据文件名推测……”，不要写成确定结论。
7. 如果发现代码中存在明显问题，可以在 README 的“已知问题 / 注意事项”中说明，但不要直接修复。
8. 如果某些功能包目前没有被 launch 调用，也要说明“当前未发现被主流程调用”。
9. 如果某些节点需要参数或依赖，请写清楚。
10. 输出内容要适合队友直接阅读，不要写成你自己的分析过程。

---

## 四、建议分阶段执行，避免一次性产生幻觉

请不要一上来直接重写 README。

请按照以下阶段执行：

---

### 阶段 1：先扫描目录结构，不修改文件

请先输出 `src` 文件夹下的功能包总览，格式如下：

```markdown
## src 功能包总览

| 功能包 | 是否 ROS 包 | 主要作用 | 关键文件 | 是否被主流程调用 |
|---|---|---|---|---|
| xxx | 是/否 | xxx | xxx | 是/否/不确定 |
```

判断 ROS 包的依据：

- 是否存在 `package.xml`
- 是否存在 `CMakeLists.txt`
- 是否包含 ROS 节点、launch、msg、srv 等

这一阶段只分析，不修改 README。

---

### 阶段 2：逐个功能包分析，不修改文件

请逐个阅读每个功能包，并输出每个功能包的分析结果。

每个功能包按照以下格式输出：

```markdown
## 功能包：package_name

### 1. 功能定位

说明这个功能包主要负责什么。

### 2. 目录结构

```text
package_name/
├── launch/
├── scripts/
├── src/
├── config/
└── ...
```

### 3. 关键节点

| 节点文件 | 节点名称 | 作用 | 发布话题 | 订阅话题 | 服务/Action |
|---|---|---|---|---|---|

### 4. 关键 launch 文件

| launch 文件 | 作用 | 启动的节点 / 包含的 launch | 关键参数 |
|---|---|---|---|

### 5. 关键配置文件

| 配置文件 | 作用 | 关键参数 |
|---|---|---|

### 6. 与其他功能包的关系

说明它依赖哪些功能包，被哪些功能包调用，和哪些话题/服务交互。

### 7. 注意事项

说明运行时需要注意的问题。
```

这一阶段仍然不要修改 README。

---

### 阶段 3：确认主流程

请根据 launch 文件、主控脚本和配置文件，整理项目主流程。

重点回答：

1. 比赛/演示时推荐启动哪个 launch 文件？
2. 主控节点是哪一个？
3. 小车从启动到完成任务的大致流程是什么？
4. 识别板一由哪个节点负责？
5. 识别板二由哪个节点负责？
6. 导航目标由哪个节点发送？
7. 语音播报由哪个模块负责？
8. TCP / 裁判系统通信由哪个模块负责？
9. 双车协作相关逻辑在哪些文件中？
10. 调试工具在哪些功能包中？

请用流程图文本表示，例如：

```text
启动 launch
    ↓
加载参数 config
    ↓
启动导航 / 视觉 / 主控节点
    ↓
前往识别板一
    ↓
识别二维码任务
    ↓
前往体检区取样
    ↓
识别板二状态判断
    ↓
前往化验区配送
    ↓
返回起点 / 下一轮
```

---

### 阶段 4：生成 README 修改方案

在真正修改 README 之前，请先输出你计划写入 README 的大纲。

README 建议包含以下章节：

```markdown
# 智慧药房 ROS 工作空间 src 功能包说明

## 1. 项目简介

## 2. 功能包总览

## 3. 系统总体架构

## 4. 主流程说明

## 5. 功能包详细说明

### 5.1 package_a

### 5.2 package_b

### 5.3 package_c

## 6. 主要话题与数据流

## 7. 主要 launch 文件

## 8. 常用运行命令

## 9. 常用调试命令

## 10. 配置文件说明

## 11. 双车协作说明

## 12. 常见问题与排查方法

## 13. 开发与维护建议
```

如果你认为实际项目更适合其他结构，可以自行调整，但必须保证逻辑清楚。

---

### 阶段 5：修改 README.md

在完成前面分析后，再修改当前文件夹下的 `README.md` 文件。

要求：

1. 使用 Markdown 格式。
2. 标题层级清晰。
3. 使用表格整理功能包、话题、launch、配置文件。
4. 使用代码块给出运行命令。
5. 使用文本流程图说明主流程。
6. 不要写过多空话。
7. 内容要适合新队友快速上手。
8. 如果某些命令无法确认，请写成“根据当前文件推测，可能使用如下命令”，不要写成绝对正确。
9. 如果存在多个启动入口，请区分：
   - 单车运行
   - 双车运行
   - 调试运行
   - 仿真运行
10. 如果 README 中已有内容正确，请保留并优化；如果已有内容错误，请修正。

---

## 五、README 中必须重点写清楚的内容

请特别关注以下内容：

### 1. 功能包总览

用表格说明：

```markdown
| 功能包 | 作用 | 关键节点 | 关键 launch | 备注 |
|---|---|---|---|---|
```

---

### 2. 系统主流程

请整理成类似：

```text
起点
  ↓
识别板一识别二维码任务
  ↓
根据二维码确定体检窗口和化验窗口
  ↓
前往体检区 A/B/C 取样
  ↓
前往识别板二判断化验区状态
  ↓
根据识别板二状态等待或通行
  ↓
前往化验区 1/2/3/4 配送
  ↓
返回起点
  ↓
进入下一轮
```

如果代码中存在双车协作，则补充：

```text
车 1 先出发
车 2 在起点等待
车 1 配送完成并开始返回时放行车 2
车 2 配送完成并开始返回时放行车 1
两车循环执行
```

---

### 3. 主要话题

请根据实际代码整理，例如：

```markdown
| 话题 | 类型 | 发布者 | 订阅者 | 作用 |
|---|---|---|---|---|
| /current_task | std_msgs/String | xxx | xxx | 当前任务状态 |
| /cv2_result | std_msgs/String | xxx | xxx | 视觉识别结果 |
| /current_qr_task | std_msgs/String | xxx | xxx | 双车任务占用广播 |
| /all_qrcodes | std_msgs/String | xxx | xxx | 识别板一全部二维码结果 |
| /dual_car_signal | std_msgs/String | xxx | xxx | 双车轮流出发控制 |
```

只写实际存在的话题，不要编造。

---

### 4. 主要 launch 文件

请整理：

```markdown
| launch 文件 | 作用 | 常用命令 | 关键参数 |
|---|---|---|---|
```

例如：

```bash
roslaunch package_name launch_file.launch
```

如果需要 `car_id`、`audio_dir`、`map_file` 等参数，请写清楚。

---

### 5. 配置文件说明

请整理：

```markdown
| 配置文件 | 作用 | 关键字段 |
|---|---|---|
| config/strategy.yaml | 策略参数 | round_return_to_start, dual_car_enabled |
| config/tcp.yaml | TCP 上报配置 | car_id, host, port |
```

字段必须来自实际文件。

---

### 6. 双车协作说明

如果当前代码包含双车协作，请重点说明：

1. 是否默认启用。
2. 如何设置 `car_id`。
3. 车 1 和车 2 如何启动。
4. `/dual_car_signal` 的作用。
5. `/current_qr_task` 的作用。
6. 两者不能混用。
7. 双车循环逻辑。
8. 远程任务预留接口是否默认关闭。
9. 调试方法。

示例结构：

```markdown
## 双车协作说明

### 1. 基本逻辑

车 1 默认先出发，车 2 在起点等待。当前车完成配送并开始返回起点时，通过 `/dual_car_signal` 放行另一辆车。

### 2. 话题职责

| 话题 | 职责 |
|---|---|
| /dual_car_signal | 控制哪辆车可以出发 |
| /current_qr_task | 广播当前占用的二维码任务 |
| /all_qrcodes | 共享识别板一全部二维码结果 |

### 3. 启动示例

```bash
# 车 1
roslaunch xxx xxx.launch car_id:=1

# 车 2
roslaunch xxx xxx.launch car_id:=2
```

### 4. 注意事项

- 不要把 `/current_qr_task` 当作出发控制信号。
- 单车模式下不要等待双车信号。
- 远程任务跳过识别板一功能如果存在，应默认关闭。
```

---

### 7. 常见问题排查

请根据代码和项目特点补充常见问题，例如：

````markdown
## 常见问题

### 1. roslaunch 找不到功能包

检查是否 source 了工作空间：

```bash
source ~/robot_ws/devel/setup.bash
```

### 2. 小车无法导航

检查：

```bash
rostopic echo /map
rostopic echo /scan
rosrun tf view_frames
```

### 3. 视觉识别没有输出

检查摄像头和话题：

```bash
rostopic list
rostopic echo /cv2_result
```

### 4. 双车不能轮流出发

检查：

```bash
rostopic echo /dual_car_signal
rostopic echo /current_qr_task
```
````

---

## 六、请特别避免的问题

请避免以下错误：

1. 把不存在的功能包写进 README。
2. 把测试脚本写成正式主流程。
3. 把调试 launch 写成比赛启动 launch。
4. 把 `/current_qr_task` 写成双车启动控制话题。
5. 把远程任务跳过识别板一写成默认启用。
6. 忽略单车模式兼容性。
7. 编造不存在的参数。
8. 编造不存在的节点名。
9. 编造不存在的启动命令。
10. 删除 README 中已有且正确的内容。

---

## 七、最终输出要求

请最终完成以下输出：

1. 简要说明你扫描到了哪些功能包。
2. 简要说明你对 README 做了哪些修改。
3. 给出修改后的 README.md 完整内容。
4. 如果你实际修改了文件，请说明修改文件路径。
5. 如果有无法确认的内容，请单独列出“待人工确认项”。

---

## 八、如果信息不足

如果你无法确认某些功能，请不要猜测。请在 README 中写：

```markdown
> 注意：该部分根据文件名和调用关系推测，具体运行方式需要结合实车测试确认。
```

或者在最后添加：

```markdown
## 待确认项

- xxx launch 是否为比赛正式入口仍需实车确认。
- xxx 节点是否仍在当前主流程中使用需要进一步测试。
```

---

## 九、本次任务的核心原则

请记住：

```text
只整理文档，不改功能代码。
基于实际文件，不凭空编造。
先分析，再写 README。
保留单车和双车说明。
让新队友能快速理解 src 目录下每个功能包的作用和运行方式。
```
